package assembler;

/**
 * Denovo sequence assembler class
 */
public class DenoSequenceAssembler implements SequenceAssembler{

    @Override
    public String assemble(DeBruijnGraph graph) {
        //TODO traverse the graph and generate the assembled sequences

        return null;
    }
}
